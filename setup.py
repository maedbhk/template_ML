from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a template for a predictive modeling project. You have some features (X) and want to predict some target (y).',
    author='Maedbh King',
    license='MIT',
)
